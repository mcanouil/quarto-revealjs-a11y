project:
  type: default
  pre-render:
    - cp -R ../_extensions _extensions
  post-render:
    - rm -rf _extensions
  render:
    - _a11y-fixture.qmd
    - _fragments-fixture.qmd
    - _fragments-together-fixture.qmd
    - _high-contrast-fixture.qmd
